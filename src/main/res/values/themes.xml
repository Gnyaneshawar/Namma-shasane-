<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. Use AppCompat as it is more compatible for the host activity. -->
    <style name="Theme.NammaShasana" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#F4EBD0</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.NammaShasana</item>
    </style>
</resources>